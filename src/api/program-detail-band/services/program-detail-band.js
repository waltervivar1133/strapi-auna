'use strict';

/**
 * program-detail-band service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::program-detail-band.program-detail-band');
