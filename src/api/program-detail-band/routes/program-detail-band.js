'use strict';

/**
 * program-detail-band router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::program-detail-band.program-detail-band');
