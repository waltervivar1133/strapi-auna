'use strict';

/**
 * program-detail-band controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::program-detail-band.program-detail-band');
